# ── browser-local branch ──────────────────────────────────────────────────────
# The server only proxies URLs and serves the SPA.
# All AI (embeddings + LLM) runs in the browser via WebGPU / Transformers.js.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0
python-multipart>=0.0.12
